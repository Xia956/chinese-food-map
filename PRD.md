# 中国美食地图网页 PRD

## 1. 项目背景

本项目是一个基于《舌尖上的中国》气质与内容方向的中文中国美食地图网页。首页以中国地图为核心视觉与交互入口，用户通过点击省份进入区域探索，并以城市、美食、方言音频和“味觉胶片”轮播的方式感受不同地域的饮食文化。

## 2. 产品目标

- 让地图成为首页主角，建立强烈的地域探索感。
- 通过平滑推进、居中放大、高亮弱化等地图动效，增强点击省份后的沉浸感。
- 让每个省份拥有声音、城市和美食内容，形成“看地图、听方言、逛美食”的体验闭环。
- 用“味觉胶片”美食轮播承载省份代表美食，营造纪录片式、慢浏览的视觉节奏。
- 支持用户点击美食卡片查看详情，进一步了解菜品、食材、故事和来源。

## 3. 目标用户

- 对中国地域美食感兴趣的普通用户。
- 喜欢《舌尖上的中国》式饮食文化叙事的用户。
- 希望通过地图快速了解各地代表美食、城市和风味的人。

## 4. 核心体验概述

用户进入首页后，首先看到一张占据页面主要视觉区域的中国地图。用户点击某个省份后，地图平滑推进，将该省份移动到视觉中心并放大。当前省份高亮显示，其他省份降低视觉权重。与此同时，页面展示该省份相关城市名称，播放该省份代表方言音频，并在地图下方或前景显示“味觉胶片”美食轮播。轮播默认自动慢速滚动，用户可按住鼠标左右拖动，轮播会跟随拖动方向移动，并根据拖动速度产生惯性。点击某道美食卡片后，弹出详情窗口。

## 5. 功能需求

### 5.1 首页中国地图

首页必须以中国地图作为主视觉，地图应占据页面最重要区域。

要求：

- 地图应清晰展示中国省级行政区域边界。
- 默认状态下，全国地图完整可见。
- 省份应具备可点击状态。
- 鼠标悬停省份时，应有轻微视觉反馈。
- 地图不应被大面积文字、卡片或装饰遮挡。

### 5.2 省份点击与地图推进

用户点击某个省份后，地图进入省份聚焦状态。

要求：

- 地图应平滑推进，不允许瞬间跳变。
- 被点击省份应移动到页面视觉中心。
- 被点击省份应适度放大。
- 推进过程应自然、克制，符合纪录片式网页气质。
- 用户点击其他省份时，应从当前省份平滑过渡到新省份。

### 5.3 当前省份高亮与其他省份弱化

进入省份聚焦状态后，应突出当前省份。

要求：

- 当前省份使用高亮样式。
- 其他省份降低透明度、饱和度或对比度。
- 弱化后的省份仍应保留地图整体轮廓。
- 当前省份名称应清晰可识别。

### 5.4 城市名称同步显示

省份被选中后，应同步显示该省份相关城市名称。

要求：

- 城市名称应跟随当前省份切换。
- 城市名称可显示在地图省份内部、周边标注层，或独立信息区域。
- 城市名称数量应适中，避免遮挡地图主体。
- 城市名称应优先展示与美食数据相关的城市。

### 5.5 方言音频播放

用户点击省份时，应播放一段该省份代表方言音频。

要求：

- 每个有音频资源的省份应配置一个代表方言音频。
- 点击省份后自动播放对应音频。
- 切换省份时，上一段音频应停止或淡出，新音频开始播放。
- 音频播放失败时，不应阻塞地图交互。
- 页面应提供明显但克制的音频开关或静音控制。
- 若浏览器限制自动播放，应在用户首次点击后再触发音频播放。

### 5.6 “味觉胶片”美食轮播

省份被选中后，应在当前省份下方或地图前景显示“味觉胶片”美食轮播。

要求：

- 轮播应以横向胶片形式展示该省份相关美食卡片。
- 每张卡片至少包含美食图片、名称和城市或地区。
- 轮播位置可以在地图下方，也可以作为前景层覆盖在地图下半部分，但不能遮挡当前省份核心区域。
- 轮播应与当前省份同步切换。
- 若当前省份没有美食数据，应显示中文空状态。

### 5.7 自动慢速滚动

味觉胶片轮播默认应自动慢速滚动。

要求：

- 自动滚动速度应缓慢、稳定，适合浏览。
- 鼠标悬停时可以保持滚动，也可以轻微减速，但不应突兀停止。
- 页面切换省份后，轮播应切换到对应省份数据并继续自动滚动。

### 5.8 鼠标拖动轮播

用户按住鼠标左右拖动时，轮播应跟随鼠标方向移动。

要求：

- 鼠标按下并横向移动时，轮播位置应实时响应。
- 向左拖动时，轮播内容应向左移动；向右拖动时，轮播内容应向右移动。
- 拖动过程中应暂停或覆盖自动滚动。
- 移动端可扩展为触摸拖动。

### 5.9 拖动惯性与恢复自动滚动

轮播应根据拖动速度产生惯性。

要求：

- 用户拖动越快，松手后的惯性越强。
- 松手后，轮播应继续按拖动方向滑行一段距离。
- 惯性速度应逐渐衰减，不应突然停止。
- 惯性结束后，轮播应自然恢复默认自动慢速滚动。
- 惯性与自动滚动之间的过渡应平滑。

### 5.10 美食详情弹窗

用户点击某道美食卡片后，应弹出详情窗口。

要求：

- 弹窗应展示该美食的图片、名称、城市或地区、主要食材、风味特征、故事和来源信息。
- 如果该美食与《舌尖上的中国》相关，应展示季数和集数。
- 信息不确定时，应明确显示“待核实”或类似状态。
- 弹窗应支持关闭。
- 点击弹窗外部或按 `Esc` 键应关闭弹窗。
- 弹窗在移动端应适配小屏幕，不遮挡关闭入口。

## 6. 数据需求

### 6.1 省份数据

每个省份建议包含：

- 省份名称
- 地图区域标识
- 代表城市列表
- 方言音频路径
- 省份简介
- 相关美食列表

### 6.2 城市数据

城市数据建议包含：

- 城市名称
- 所属省份
- 经纬度或地图标注位置
- 相关美食 ID 列表

### 6.3 美食数据

美食数据建议包含：

- 美食 ID
- 美食名称
- 所属省份
- 所属城市或地区
- 图片
- 主要食材
- 风味特征
- 食物故事
- 文化背景
- 《舌尖上的中国》季数与集数
- 来源链接或来源说明
- 可信度或核实状态

## 7. 交互流程

1. 用户进入首页，看到完整中国地图。
2. 用户悬停省份，省份出现轻微反馈。
3. 用户点击省份。
4. 地图平滑推进，将该省份居中放大。
5. 当前省份高亮，其他省份弱化。
6. 页面同步显示该省份相关城市名称。
7. 播放该省份代表方言音频。
8. 页面显示该省份“味觉胶片”美食轮播。
9. 轮播默认自动慢速滚动。
10. 用户按住并左右拖动轮播，轮播跟随移动。
11. 用户松手后，轮播根据拖动速度产生惯性并逐渐减速。
12. 惯性结束后，轮播恢复自动慢速滚动。
13. 用户点击美食卡片。
14. 弹出美食详情窗口。
15. 用户关闭弹窗后，回到省份探索状态。

## 8. 视觉与动效要求

- 地图是页面主角，其他元素应服务地图探索。
- 动效应平滑、沉稳，不追求夸张炫技。
- 当前省份高亮应清楚但不刺眼。
- 其他省份弱化后仍应保留地理上下文。
- “味觉胶片”应有横向影像带的感觉，但避免过度拟物。
- 图片卡片应统一比例，避免轮播过程中布局跳动。
- 弹窗应具有清晰层级，背景可轻微压暗。

## 9. 音频要求

- 音频应短小、清晰，适合作为省份氛围提示。
- 音量默认不应过大。
- 用户应能关闭或重新开启音频。
- 切换省份时，音频切换应自然。
- 如果某省份缺少音频，不应播放占位音或报错。

## 10. 响应式要求

- 桌面端地图应完整、宽阔，轮播可位于地图下方或前景下部。
- 移动端地图仍应是首屏核心，但可简化城市标注数量。
- 移动端轮播应支持触摸拖动。
- 弹窗在移动端应改为适合阅读的底部面板或全屏面板。
- 所有中文文本不得重叠、溢出或被裁切。

## 11. 非功能需求

- 地图交互应保持流畅。
- 图片应压缩并使用合适尺寸。
- 音频资源应按需加载，避免首屏过重。
- 数据结构应清晰，方便后续补充省份、美食和音频。
- 不得因音频或图片加载失败影响核心地图交互。

## 12. 验收标准

- 首页打开后，中国地图是页面最醒目的主体。
- 点击任一配置省份后，地图会平滑居中放大该省份。
- 当前省份高亮，其他省份明显弱化。
- 省份切换时，城市名称、美食轮播和音频同步更新。
- 点击省份后能播放对应方言音频，且用户可关闭声音。
- 味觉胶片轮播默认自动慢速滚动。
- 用户按住鼠标左右拖动时，轮播能实时跟随。
- 拖动速度越快，松手后的惯性越明显。
- 惯性逐渐减速后，轮播能恢复自动滚动。
- 点击美食卡片后，能打开详情弹窗。
- 弹窗可通过关闭按钮、点击外部或 `Esc` 键关闭。
- 移动端无明显遮挡、重叠或文字裁切。

## 13. 后续可扩展方向

- 增加按季数、集数、食材、风味筛选。
- 增加搜索功能，支持搜索菜名、城市和省份。
- 增加路线模式，将多个省份串成美食旅程。
- 增加节目片段来源索引，但必须遵守版权与引用规则。
- 增加用户收藏或分享单个美食详情的能力。

