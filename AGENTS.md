# AGENTS.md

## 项目概览

本项目是一个受纪录片《舌尖上的中国》启发的中国美食地图网页。网站应帮助用户通过清晰、尊重且可交互的地图体验，探索中国各地美食、食材、地域文化故事，以及与节目相关的可验证信息。

默认产品方向：

- 首屏呈现真正可用的美食地图或探索界面，而不是营销落地页。
- 内容优先，信息准确，方便浏览和筛选。
- 以有分寸的方式呈现食物、地域、人群和文化，不制造刻板印象。
- 网站提供 `/zh` 中文与 `/en` 英文两套界面，共享同一份结构化数据；中文是内容维护和事实核验的基准语言。

## 核心规则

### 内容真实性

- 不得虚构菜品、地点、节目集数、节目出处、人物、引文或历史说法。
- 如果某道菜与《舌尖上的中国》相关，只有在确认后才能记录季数、集数和来源。
- 信息不确定时，应明确标记为“不确定”或“待核实”，不能写成事实。
- 避免把地域饮食文化简单化，例如“川菜都很辣”“北方都吃面食”等。
- 美食条目应优先使用结构化、可人工编辑的数据文件维护。

### 版权与来源

- 不得复制纪录片解说词、文章、书籍或网页中的大段文字。
- 可以在识别、评论或背景说明中短引用节目相关信息，但必须克制。
- 未经用户明确授权，不得使用纪录片截图、剧照、视频片段或受版权保护的图片。
- 优先使用原创文案、公有领域资源、开放授权素材、用户提供的材料，或在适合时使用生成图片。
- 涉及外部事实依据时，应在数据中保留来源链接或来源说明字段。

### 默认数据结构

美食地图条目应使用稳定、简单、便于人工维护的数据结构。优先包含以下字段：

- `id`：唯一标识
- `name`：菜名或食物名称
- `province`：省份
- `city`：城市或地区
- `region`：大区或地理区域
- `latitude`：纬度
- `longitude`：经度
- `season`：节目季数
- `episode`：节目集数
- `category`：类别
- `ingredients`：主要食材
- `flavorProfile`：风味特征
- `story`：食物故事
- `culturalContext`：文化背景
- `image`：图片路径或图片信息
- `sources`：来源
- `confidence`：可信度或核实状态

只有当字段能支撑真实的用户功能时，才新增字段。

### 用户体验

- 首屏应直接进入地图、列表或探索体验。
- 用户应能按地理位置浏览，也应能搜索菜名、省份、城市、食材或节目集数。
- 筛选项可包括省份、区域、季数、集数、食材、风味和类别。
- 桌面端和移动端都必须具备良好的可读性和可操作性。
- 避免干扰阅读和探索的重动画。
- 空状态、加载状态和缺失数据状态都应清晰、自然。

### 视觉方向

- 使用现代、克制、带有纪录片气质的视觉语言，中英文界面保持一致的内容层级与审美。
- 默认色彩可使用温润纸色、墨黑、克制红色点缀，以及低饱和绿色或蓝灰色作为地图和界面对比。
- 避免俗套国风装饰，例如过度使用金色、灯笼、龙纹、杂乱毛笔元素或喧闹渐变。
- 需要视觉素材时，优先使用食物图片、地图纹理、地域相关图像或合适的生成位图素材。
- 布局应沉稳、清晰、内容密度适中。

### 技术默认值

- 如果项目尚未建立技术栈，优先使用轻量静态网页方案，例如 Vite、React、TypeScript 和普通 CSS 或轻量样式方案。
- 复杂地图或地理可视化应优先使用成熟库，不要手写复杂地理渲染逻辑。
- 数据应与组件分离。
- 不引入大型依赖，除非它能明显降低复杂度或提供必要的地图、数据能力。
- 优先使用语义化 HTML、响应式 CSS 和可预测的导航结构。
- 可复用常量、数据结构和数据处理函数应放在清晰命名的模块中。

### 代码修改规则

- 修改前先阅读现有项目结构。
- 项目形成约定后，应遵循已有模式。
- 改动范围应聚焦当前请求，不做无关重构。
- 未经用户明确要求，不删除用户创建的文件或数据。
- 谨慎保护人工整理的美食数据。
- 修改数据结构前，应同步更新所有受影响的渲染、筛选和文档。
- 进行有意义的代码修改后，应运行可用的检查、构建或测试。

### 文案风格

- 中文界面使用准确、有画面感但克制的中文表达；英文界面使用自然、简洁的英文，不做逐字硬译。
- 新增界面文案时必须同步维护 `src/i18n.ts` 中的中英文版本。
- 默认语气应接近纪录片式的精炼叙述。
- 除非用户明确要求，不使用夸张种草或营销式表达。
- 不浪漫化贫困、劳动、乡村生活、民族身份或地域习俗。
- 食物描述应兼顾风味、地理、手艺和人的生活背景。

### 可访问性与国际化

- 字号应清晰可读，文字与背景应有足够对比度。
- 不得只依赖颜色表达筛选状态或地图状态。
- 有意义的图片应根据当前语言提供对应替代文本。
- 中文文案应自然、准确、无生硬翻译腔。
- 英文界面标签、占位文本、帮助文本和空状态应与中文界面功能一致。
- 界面文案应放在可本地化结构中，事实数据仍以中文源数据为准。

### 完成前检查

完成工作前应确认：

- 地图或核心浏览体验仍可正常使用。
- 美食条目没有虚构信息。
- 与《舌尖上的中国》相关的内容有来源，或已标记为待核实。
- 移动端布局不会遮挡、重叠或裁切重要文字。
- 新增依赖有明确理由。
- 如项目提供构建、检查或测试命令，应确认其通过。
